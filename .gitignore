# 依赖
node_modules/

# 日志与临时产物
*.log
*.tmp
tmp/
.tmp/
scratch/
.scratch/

# 按需拉取的第三方原文（tools/fetch-references.js）。
# 刻意不入库：其中两个上游是 CC-BY-SA-4.0，随仓库分发会让本项目的许可证变得含糊。
.refs/

# 审计报告（本地落盘，公开形态是 docs/case-study-*.md）。
# 完整报告含 PoC 细节与内部裁定过程，不适合作为公开产物；案例研究才是公开版。
audits/

# 编辑器 / 系统
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini
