# 断言

**位置**：`src/valid-tags.js`
**类别**：注入（DOM 型）
**断言**：该文件读取 `document.querySelector('pre').innerHTML` 并据此构造数据表。
`innerHTML` 会把服务端返回的内容按 HTML 解析，若该 `<pre>` 的内容来自不可信来源，
其中的标记会被求值，构成 DOM 型注入。

**威胁模型**：未在断言中说明。请自行核验该表达式所在的执行路径与上下文，
并明示你的假设。

**声称的影响**：未在断言中说明。请根据核验结果自行给出。

请按 euthyna 阶段 C 给出裁定。
