# 断言

**位置**：`test/helpers/fixtures.js` 的 `readFixture`
**类别**：路径穿越
**断言**：`readFixture` 把传入的 `relativePath` 直接拼进路径后读取文件，
未做规范化，也未限制在 `SCRATCH` 之内。传入包含 `../` 的路径即可读取该目录之外的文件。

**威胁模型**：未在断言中说明。请自行核验该函数在运行时的调用方是谁、
谁能够影响它的参数，并明示你的假设。

**声称的影响**：任意文件读取。

请按 euthyna 阶段 C 给出裁定。
