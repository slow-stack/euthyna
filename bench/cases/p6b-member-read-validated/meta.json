{
  "id": "p6b-member-read-validated",
  "groundTruth": "false_positive",
  "category": "path_traversal_read",
  "why": "The same read-side claim, but archive.js rejects dataset names containing path separators before tar runs, so no member can escape data/; the output-path containment from p5b is also present, making this tree clean on both axes.",
  "establishedBy": "execution",
  "proof": "bench/exploits.js -> p6b submits the same escaping member name through the same dashboard route and asserts the handler rejects it before any tar invocation and no tarball is produced.",
  "neighbourOf": "p6-member-read-traversal",
  "neighbourNote": "Identical claim text to p6; only the guards differ."
}
